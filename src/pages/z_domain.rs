use yew::prelude::*;

#[function_component(ZDomain)]
pub fn z_domain() -> Html {
    html! {
        <>
            <h2 class="text-2xl">{ "Z-Transformed Domain" }</h2>

            <p>{ "This is the Z domain page." }</p>
            <p>{ "Implemention is coming somewhen in the future" }</p>
        </>
    }
}
